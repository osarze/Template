mixin feature-box(title, icon, url, details)
    div(class='feature-box')
        div(class='feature-box-icon')
            span(class='fa fa-' + icon )
        div(class='feature-box-content')
            h4 #{title}
            p #{details}
            a(href=url, class='btn btn-primary') View Details